# Hoppz — machine-readable session state.
# Companion to CONTEXT.md / ARCHITECTURE.md / BUILD_INDEX.md.
# Update at the end of every session.

current_phase: reskin-city-detail
last_updated: 2026-06-16
app_root: app/

last_change: >
  Reskinned City Detail screen to match Stitch reference. Header changed from
  centered city-name layout to left-aligned back + stacked "Hoppz" / city name
  with profile avatar right. Tabs restyled with uppercase label typography and
  accent bottom border. Venue list replaced with card-based layout: hotel cards
  have 80×80 icon thumbnail, title/price row, address, 5-star rating display
  (filled + unfilled), and full-width PREFER/PREFERRED button with favorite
  icon. Bar/food cards use stacked layout with title, address, and green
  pill badges for "Has Food" / "Full Bar" / descriptors. Tap-to-focus retained
  with ring-accent flash on cards. Vote FAB restyled to 56px height with
  how_to_vote icon, rounded-card, accent border/bg states, active:scale-95.
  Stars component updated with showAll prop to render all 5 stars (filled +
  unfilled outlines).

last_session: "Reskin City Detail screen to Stitch reference"

verification:
  typecheck: pass        # pre-existing module warnings only
  lint: pass             # pre-existing warnings only
  build: pass            # next build — all 44 routes compiled

known_issues:
  - App icons are placeholders — public/icons/ needs real PNGs
    (icon-192.png, icon-512.png, apple-touch-icon.png) before install looks right.
  - VAPID keys not yet generated — run npx web-push generate-vapid-keys and add
    to .env.local + Vercel env vars before push notifications will work.
  - v2_voter_notes table needs to be created in Supabase (migration pending).
  - v2_voters avatar_url and role columns need to be added in Supabase (migration pending).
  - v2_trip, v2_trip_hotels, v2_trip_hotel_assignments, v2_trip_members tables
    need to be created in Supabase (migration pending).

next_up:
  - Wire push notification triggers (new message, reaction, etc.)
  - Real PNG app icons (192 / 512 / 180)
  - Reskin remaining screens to match Stitch reference
