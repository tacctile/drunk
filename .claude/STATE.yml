# Hoppz — machine-readable session state.
# Companion to CONTEXT.md / ARCHITECTURE.md / BUILD_INDEX.md.
# Update at the end of every session.

current_phase: reskin-cities
last_updated: 2026-06-16
app_root: app/

last_change: >
  Reskinned Cities List screen to match Stitch reference. Row layout switched
  from pixel-based flex to percentage-based columns (35% city / 20% walk /
  25% distance / 15% vote) matching the Stitch 4-column pattern. Walk score
  column now stacks vertically (big display number above, grade badge pill
  below) instead of horizontal. Grade badges gained borders (grade-color/20)
  and "Grade" prefix text. Voted row gets accent/5 background tint and 4px
  left accent border. Vote buttons are now circular (rounded-full) with
  accent fill + shadow when voted, opacity-40 ghost when unvoted. All rows
  have active:scale-[0.98] tap feedback. Min row height increased 72→80px.
  Column header switched to 10px uppercase tracking-widest labels. State
  group headers match column header aesthetic. Sort control changed from
  full-width ActionBar button to floating centered pill (rounded-full,
  shadow, accent icon). No new UI components added — no reusable primitives
  were needed beyond existing library.

last_session: "Reskin Cities List screen to Stitch reference"

verification:
  typecheck: pass        # pre-existing module warnings only
  lint: pass             # pre-existing warnings only
  build: pass            # next build — all 44 routes compiled

known_issues:
  - App icons are placeholders — public/icons/ needs real PNGs
    (icon-192.png, icon-512.png, apple-touch-icon.png) before install looks right.
  - VAPID keys not yet generated — run npx web-push generate-vapid-keys and add
    to .env.local + Vercel env vars before push notifications will work.
  - v2_voter_notes table needs to be created in Supabase (migration pending).
  - v2_voters avatar_url and role columns need to be added in Supabase (migration pending).
  - v2_trip, v2_trip_hotels, v2_trip_hotel_assignments, v2_trip_members tables
    need to be created in Supabase (migration pending).

next_up:
  - Wire push notification triggers (new message, reaction, etc.)
  - Real PNG app icons (192 / 512 / 180)
  - Reskin remaining screens to match Stitch reference
