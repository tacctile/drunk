# Bar Hoppers — machine-readable session state.
# Companion to CONTEXT.md / PROGRESS.md. Update at the end of every session.

current_phase: chat-session-a
last_updated: 2026-06-14
app_root: app/            # the live Next.js v2 app; root index.html is legacy v1

last_change: >
  Chat Session A — fix double TopBar + chat core. AppShell now bypasses
  shell for /social/* routes (HopShell is the sole shell). Chat page at
  /social with message list, input bar, realtime, optimistic sends,
  pagination, day dividers, message grouping, markRead via IntersectionObserver.
  useChat hook and lib/chat.ts types/helpers created.

verification:
  typecheck: pass        # tsc --noEmit
  lint: pending          # next lint
  build: pass            # next build — all routes compiled

known_issues:
  - App icons are placeholders — public/icons/ needs real PNGs
    (icon-192.png, icon-512.png, apple-touch-icon.png) before install looks right.
  - bh2-auth is a session cookie, so a cold direct deep-link to /plan/* in a new
    browser session bounces through /login → /home once before the cookie is set.
    Intended soft-guard behavior, not a bug.
  - Camera button is a placeholder (console.log "open camera") — Session D.
  - Gallery button is a placeholder (console.log "open gallery") — Session C.
  - Image expand is a placeholder (console.log "expand image") — Session C.
  - Reactions UI not yet built — Session B.
  - Read receipts display not yet built (markRead fires, but no UI shows who read).

next_up:
  - Session B: reactions UI (emoji picker, reaction badges under bubbles)
  - Session C: image gallery + image expand modal
  - Session D: camera capture
