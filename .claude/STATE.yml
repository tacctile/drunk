# Bar Hoppers — machine-readable session state.
# Companion to CONTEXT.md / PROGRESS.md. Update at the end of every session.

current_phase: full-polish-pass
last_updated: 2026-06-14
app_root: app/            # the live Next.js v2 app; root index.html is legacy v1

last_change: >
  Full Polish Pass — Known Issues + UX Fixes.
  Verified all 13 task items against codebase. Fixed: Android install button
  opacity (disabled:opacity-50 instead of disabled:opacity-100), TopBar trip
  status pip labels (added "Today!" case and "d away" suffix), CONTEXT.md
  localStorage contract (added bh2-hopperz-view key). All other items verified
  correct as-is: login sign-in lookup, camera controls/retake/send, chat scroll,
  PlanNav/HopNav dividers and ink-dim colors, AppShell suppressNav, profile
  overlay tabs, Hopperz view persistence, Avatar fallback, Supabase voter
  select, moderator routing.

verification:
  typecheck: pass        # tsc --noEmit
  lint: pass             # next lint (only pre-existing warnings)
  build: pass            # next build — 44 routes compiled

known_issues:
  - App icons are placeholders — public/icons/ needs real PNGs
    (icon-192.png, icon-512.png, apple-touch-icon.png) before install looks right.
  - bh2-auth is a session cookie, so a cold direct deep-link to /plan/* in a new
    browser session bounces through /login → /home once before the cookie is set.
    Intended soft-guard behavior, not a bug.
  - VAPID keys not yet generated — run npx web-push generate-vapid-keys and add
    to .env.local + Vercel env vars before push notifications will work.
  - v2_voter_notes table needs to be created in Supabase (migration pending).
  - v2_voters avatar_url and role columns need to be added in Supabase (migration pending).
  - v2_trip, v2_trip_hotels, v2_trip_hotel_assignments, v2_trip_members tables
    need to be created in Supabase (migration pending).

next_up:
  - Wire push notification triggers (new message, reaction, etc.)
  - Real PNG app icons (192 / 512 / 180)
