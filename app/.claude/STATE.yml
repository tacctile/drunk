# Hoppz — machine-readable session state.
# Companion to CONTEXT.md / ARCHITECTURE.md / BUILD_INDEX.md.
# Update at the end of every session.

current_phase: docs-reconciled
last_updated: 2026-06-30
app_root: app/

last_change: >
  non-code: Reconciled all .claude/ context files against live code and
  Supabase schema. Rewrote CONTEXT.md, ARCHITECTURE.md, BUILD_INDEX.md,
  PROGRESS.md, MASTER_CONTEXT.md. Updated CHAT_CONTEXT.md and
  COMPLETION_TEMPLATES.md (name + date). Removed all references to deleted
  components (TripPage, VotePage, DatesPage, WalkStrip, ConstellationMap,
  score.ts, geo.ts, usePlaces.ts), old flat routes (/cities, /calendar,
  /board, /locate), stale "Bar Hoppers" branding, and the "light mode
  equally finished" claim. All 17 v2_* tables verified present in live
  schema. Zero source code changes. Zero Supabase writes.

last_session: "non-code: reconcile all .claude/ docs to match live code and schema"

verification:
  typecheck: not-run      # non-code session — no source changes
  lint: not-run
  build: not-run

known_issues:
  - App icons are placeholders — public/icons/ needs real PNGs
    (icon-192.png, icon-512.png, apple-touch-icon.png) before install looks right.
  - VAPID keys not yet generated — run npx web-push generate-vapid-keys and add
    to .env.local + Vercel env vars before push notifications will work.

next_up:
  - Wire push notification triggers (new message, reaction, etc.)
  - Real PNG app icons (192 / 512 / 180)
  - Reskin remaining screens to match Stitch reference
